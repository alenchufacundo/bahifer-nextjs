"use client";

// import PhotoGrid from "@/app/components/common/PhotoGrid";
import { Box, Container } from "@mui/material";
import { useState } from "react";
import { useFormik } from "formik";
import DataGridComponent from "./components/DataGridComponent";

function Abrasivos() {
  const options = [
    { label: "Opción 1", value: "opcion1" },
    { label: "Opción 2", value: "opcion2" },
    // ... otras opciones
  ];

  console.log(options[0]);

  // Supongamos que tienes un estado para manejar la selección
  const [selectedValue, setSelectedValue] = useState("");

  // Supongamos que también tienes una función para manejar el cambio en la selección
  const handleSelectChange = (value) => {
    setSelectedValue(value);
  };

  const { handleSubmit, values, setFieldValue } = useFormik({
    initialValues: {
      id: "",
      descripcion: "",
      sucursal: "",
      provincia: "",
      localidad: "",
    },

    onSubmit: () => {
      console.log(values);
    },
  });
  return (
    <Container
      maxWidth="xl"
      sx={{ marginTop: "30px", backgroundColor: "#fff" }}
    >
      {/* <PhotoGrid /> */}\
      <Box component="form" onSubmit={handleSubmit}>
        <DataGridComponent />
      </Box>
    </Container>
  );
}

export default Abrasivos;
